.tropes-page {

  center-page()

  .tropes-container {

    $tile-size = 200;
    $tile-margin = 30;

    float: left;
    trope-tile($tile-size, $tile-size);

    .trope-tile:hover {
      cursor: pointer;
    }

    .trope-tile, .text-tile {
      float: left;
      margin-right: unit($tile-margin, 'px');
      margin-bottom: unit($tile-margin - 5, 'px');
    }

    .text-tile.tropes {
      display: inline-block;
      position: relative;
      overflow: hidden;
      max-width:  unit($tile-size * 2 + $tile-margin, 'px');
      /*height: unit($tile-size * 2 + ($tile-margin - 5) , 'px');*/
      height: unit($tile-size , 'px');
      @media screen and (max-width:$mobile-width) {
        min-height: 100%;
      }

      site-title-styles()

      h2.site-title {
        /*font-size: 2em;*/
        padding-left: 0px;
      }

      a.gender-filter {
        padding: 1.5px 7px;
        color: white;
        text-decoration: none;
        cursor: pointer;
        &.m {
          background-color: $male-col;
        }

        &.f {
          background-color: $female-col;
        }
      }
    }

    .text-tile.filter {
      width:  unit($tile-size, 'px');
      height: unit($tile-size, 'px');
      a {
        cursor: pointer;
        padding: 1.5px 7px;
        color: white;
        text-decoration: none;

        &.f {
          background-color: $female-col;
        }

        &.m {
          background-color: $male-col;
        }
      }

      a:hover {
        color: white;
        background-color: $highlight-col;
      }

      p {
        margin: 0;

        font-size: 16px;
        font-weight: 300;
        line-height: 26px;
        padding-right: 15px;

      }
    }

    .text-tile.navTile {
      width:  unit($tile-size, 'px');
      height: unit($tile-size, 'px');
      a {
        cursor: pointer;
      }

      a:hover {
        color: white;
        background-color: $highlight-col;
      }

      p {
        margin: 0;

        font-size: 16px;
        font-weight: 300;
        line-height: 26px;
        padding-right: 15px;

      }
    }

  }
}
